<Window x:Class="PokerNoteManager.Vision.NotePopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Poker Notes"
        Width="360" SizeToContent="Height"
        WindowStyle="ToolWindow" ResizeMode="NoResize" ShowInTaskbar="False"
        WindowStartupLocation="Manual" Topmost="True"
        Background="#0F172A" Foreground="#E2E8F0"
        FontFamily="Segoe UI, Inter, Arial" FontSize="12"
        TextOptions.TextFormattingMode="Display" UseLayoutRounding="True">

    <Window.Resources>
        <Style x:Key="PopupButton" TargetType="{x:Type Button}">
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Foreground" Value="#E2E8F0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Bg" Background="#1E293B" BorderBrush="#334155" BorderThickness="1"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#243449" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PopupSave" TargetType="{x:Type Button}" BasedOn="{StaticResource PopupButton}">
            <Setter Property="Foreground" Value="#FFFFFF" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Padding" Value="16,7" />
            <Setter Property="MinWidth" Value="104" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Bg" Background="#15803D" BorderBrush="#22C55E" BorderThickness="1"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#16A34A" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#166534" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PopupText" TargetType="{x:Type TextBox}">
            <Setter Property="Background" Value="#111827" />
            <Setter Property="Foreground" Value="#E2E8F0" />
            <Setter Property="CaretBrush" Value="#22C55E" />
            <Setter Property="BorderBrush" Value="#334155" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="SelectionBrush" Value="#22C55E" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PopupLabel" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="#64748B" />
            <Setter Property="FontSize" Value="10" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Margin" Value="0,9,0,4" />
        </Style>
    </Window.Resources>

    <Border BorderBrush="#334155" BorderThickness="1" Padding="11">
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="CaptionText" Grid.Column="0" FontWeight="Bold" FontSize="13"
                           Foreground="#22C55E" TextWrapping="Wrap" VerticalAlignment="Center" />
                <Button x:Name="BtnClose" Grid.Column="1" Style="{StaticResource PopupButton}" Content="✕"
                        Padding="7,2" Margin="8,0,0,0" ToolTip="Close (Esc)" Click="Close_Click" />
            </Grid>

            <TextBlock Style="{StaticResource PopupLabel}" Text="PLAYER" />
            <TextBox x:Name="NameBox" Style="{StaticResource PopupText}" />
            <TextBlock x:Name="NameHint" Foreground="#64748B" FontSize="10" Margin="0,3,0,0"
                       TextWrapping="Wrap" Visibility="Collapsed" />

            <TextBlock Style="{StaticResource PopupLabel}" Text="TAGS" />
            <WrapPanel x:Name="TagPanel" />
            <TextBlock x:Name="NoTagsHint" Foreground="#64748B" FontSize="10" Margin="0,2,0,0"
                       Text="No tags defined yet - create them under Tags in the main window." Visibility="Collapsed" />

            <TextBlock Style="{StaticResource PopupLabel}" Text="NOTE" />
            <TextBox x:Name="NoteBox" Style="{StaticResource PopupText}" Height="104" AcceptsReturn="True"
                     TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" />

            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="HintText" Grid.Column="0" Foreground="#64748B" FontSize="10"
                           VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="0,0,8,0"
                           Text="Ctrl+Enter saves · Esc closes" />
                <Button x:Name="BtnOpen" Grid.Column="1" Style="{StaticResource PopupButton}" Content="Open"
                        MinWidth="66" Margin="0,0,8,0" ToolTip="Open this player in the main window"
                        Click="Open_Click" />
                <Button x:Name="BtnSave" Grid.Column="2" Style="{StaticResource PopupSave}" Content="✔  Save"
                        Click="Save_Click" />
            </Grid>
        </StackPanel>
    </Border>
</Window>
